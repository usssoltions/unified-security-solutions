{
  "name": "Vendor",
  "type": "object",
  "description": "Estate vendor/service provider. TENANT ACCESS: every tenant read/write goes through the estateAccess backend gateway (manager CRUD + vendor self-service scoped to the linked vendor record); direct client access is restricted to platform admins by RLS. NO PAYMENT FUNCTIONALITY \u2014 the app never collects fees, charges or payments; catalogue item prices are informational display values only.",
  "properties": {
    "customer_id": {
      "type": "string",
      "description": "Tenant/customer ownership for multi-tenant isolation."
    },
    "reseller_id": {
      "type": "string",
      "description": "Reseller ownership for reseller-scoped oversight access."
    },
    "user_id": {
      "type": "string",
      "description": "Linked vendor portal User ID \u2014 vendor self-service scope is resolved from this link server-side."
    },
    "business_name": {
      "type": "string"
    },
    "contact_name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "whatsapp": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "enum": [
        "restaurant",
        "shop",
        "laundry",
        "plumbing",
        "electrical",
        "landscaping",
        "cleaning",
        "security",
        "other"
      ]
    },
    "description": {
      "type": "string"
    },
    "logo_url": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "suspended"
      ],
      "default": "active"
    },
    "rating": {
      "type": "number"
    },
    "operating_hours": {
      "type": "string"
    },
    "delivery_available": {
      "type": "boolean",
      "default": false,
      "description": "Logistics capability flag only (offers delivery vs pickup). NO fee is attached to delivery \u2014 the app has no payment functionality."
    },
    "minimum_order": {
      "type": "number",
      "default": 0,
      "description": "Informational display value only \u2014 not enforced, charged or collected."
    },
    "bank_account": {
      "type": "string",
      "description": "Vendor directory information for the estate manager's reference. Display-only \u2014 the app never processes payments."
    },
    "vat_number": {
      "type": "string",
      "description": "Vendor registration/directory information. Display-only."
    }
  },
  "required": [
    "business_name",
    "category",
    "phone"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    }
  }
}