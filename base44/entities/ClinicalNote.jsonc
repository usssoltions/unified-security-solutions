{
  "name": "ClinicalNote",
  "type": "object",
  "description": "Medical module: Therapist clinical notes. CLINICAL \u2014 reception and employer portal users have ZERO access through the medicalAccess gateway; therapists act on their own notes, practice admins oversee. Restricted notes are visible to their author and practice admins only. Amendment history preserved; unrestricted deletion not permitted (not offered through the gateway). Audited. GATEWAY-ONLY ACCESS: direct client access is restricted to platform administration (built-in role 'admin' and admin_level 'platform') by RLS.",
  "properties": {
    "customer_id": {
      "type": "string"
    },
    "reseller_id": {
      "type": "string"
    },
    "patient_id": {
      "type": "string"
    },
    "patient_name": {
      "type": "string"
    },
    "session_id": {
      "type": "string"
    },
    "therapist_id": {
      "type": "string"
    },
    "therapist_name": {
      "type": "string"
    },
    "note_type": {
      "type": "string",
      "enum": [
        "assessment",
        "observation",
        "intervention",
        "goal",
        "follow_up",
        "general"
      ],
      "default": "general"
    },
    "content": {
      "type": "string",
      "description": "Clinical note text"
    },
    "amendment_history": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "timestamp": {
            "type": "string",
            "format": "date-time"
          },
          "edited_by_id": {
            "type": "string"
          },
          "edited_by_name": {
            "type": "string"
          },
          "previous_content": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          }
        }
      },
      "default": []
    },
    "created_at_note": {
      "type": "string",
      "format": "date-time"
    },
    "is_restricted": {
      "type": "boolean",
      "default": false,
      "description": "If true, only the author and practice admin can view"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "archived"
      ],
      "default": "active"
    }
  },
  "required": [
    "customer_id",
    "patient_id",
    "therapist_id",
    "content"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "admin_level": "platform"
          }
        }
      ]
    }
  }
}