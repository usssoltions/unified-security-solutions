{
  "name": "Notification",
  "type": "object",
  "description": "In-app notification record. CANONICAL REGISTRY: producers must use a type from this schema's enum and a priority of low/medium/high/critical only \u2014 the legacy 'normal' priority is invalid and normalizes to 'medium'. One notification record PER RECIPIENT (multi-recipient dispatches create one record each; a shared record leaves recipients with no in-app entry).",
  "properties": {
    "customer_id": {
      "type": "string"
    },
    "reseller_id": {
      "type": "string"
    },
    "recipient_id": {
      "type": "string"
    },
    "recipient_name": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "incident_reported",
        "incident_assigned",
        "incident_accepted",
        "incident_declined",
        "incident_resolved",
        "incident_critical",
        "maintenance_reported",
        "maintenance_assigned",
        "maintenance_accepted",
        "maintenance_declined",
        "maintenance_completed",
        "status_change",
        "alarm_dispatch",
        "shift_reminder",
        "shift_handover",
        "training_assigned",
        "task_assigned",
        "task_reassigned",
        "patrol_due",
        "patrol_overdue",
        "missed_checkin",
        "low_battery",
        "laundry_request",
        "assignment",
        "system"
      ]
    },
    "priority": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ],
      "default": "medium"
    },
    "title": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "read": {
      "type": "boolean",
      "default": false
    },
    "read_at": {
      "type": "string",
      "format": "date-time"
    },
    "related_entity": {
      "type": "string"
    },
    "related_id": {
      "type": "string"
    },
    "action_url": {
      "type": "string"
    },
    "sent_via": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "push",
          "email",
          "telegram",
          "in_app"
        ]
      },
      "default": [
        "in_app"
      ]
    }
  },
  "required": [
    "recipient_id",
    "type",
    "title",
    "message"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "admin_level": "platform"
          }
        },
        {
          "data.recipient_id": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "admin_level": "platform"
          }
        },
        {
          "data.recipient_id": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "admin_level": "platform"
          }
        },
        {
          "data.recipient_id": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "user_condition": {
        "admin_level": "platform"
      }
    }
  }
}